# Generate_Image_with_pure_C_CPlusPlus
Generate an image without any graphican library. Just pure c/c++ code. 
## How it works
It generates a/some ppm image. And the way ppm image works is-> A PPM IMAGE is just a grid of pixels, and the program loops over every (x, y) position and decides what color that pixel should be based on some rules or math. 
